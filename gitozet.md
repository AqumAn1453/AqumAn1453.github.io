git config --global user.name "adýn soyadýn"   = adýný belirle
git config --global user.email "email"  = emailini belirle
**
cd //kodlarý kaydettiðin doyayýnýn dizilimin// = kodlarýn nereden aldýðýn belli olmasý için
git clone "github daki Clone with HTTPS" = githubdaki dosyalarý pcndeki dosyaya almak için
**
git init = (master olmasý için)
**
ls -a = git deki dosyalarý görmek için kullanýlýr.
**
git add "kod dosyasýnýn adý.md" = hangi dosyayý githuba atcaðýmýzý beliriyoruz.
git commit -m "baþlýk" = dosyayý git huba atmak için baþlýk örn(kaçýncý çalýþman, hangi çalýþman)
git push = dosyayý onaylam için.
**